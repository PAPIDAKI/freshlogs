
<h3>Listing Workers</h3>

<%=link_to"add Worker",new_tenant_worker_path(@tenant)%>
<h5>Import Workers</h5>
<%= form_tag import_tenant_workers_path, multipart: true do %>
  <%= file_field_tag :file %>
  <div>
  <%= submit_tag "Import",class:'import' %>
  </div>
<% end %>

<div class='media'>
  <div class='media-body'>
  <table>

                    <table class='table table-hover'>
                        <tbody>
                           <% @workers.each do |worker|%>
                              <tr>
                                 <td><%=link_to worker.name,edit_tenant_worker_path(@tenant,worker)%></td>
                                 <td><%=worker.mobile %></td>
                                 <td><%= worker.kind%></td>
                                 <td><%= worker.work_for%></td>
                                 <td><%=worker.status %></td>
                               </tr>
                              <%end%>
                       </tbody>
                    </table>

                      <!--  <thead>Header test </thead>
           <%if @workers_hash %>
            <% @workers_hash.each do |k,v| %>
            <h4><%=k%></h4>
                    <table class='table table-hover'>
                        <tbody>
                           <% v.each do |worker|%>
                              <tr>
                                 <td><%= worker.kind%></td>
                                 <td><%=link_to worker.name,edit_tenant_worker_path(@tenant,worker)%></td>
                                 <td><%=worker.mobile %></td>
                                 <td><%=worker.status %></td>
                               </tr>
                              <%end%>
                       </tbody>
                    </table>
                 <%end%>
          <%end%> -->


             
              
          </table>
  </div>
</div>




