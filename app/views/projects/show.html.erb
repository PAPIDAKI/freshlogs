<%- model_class = Project -%>
<div class="page-header">
  <h3><%= @project.title %></h3>
  <h3><small>Organization - <%= tenant_name @project.tenant_id %></small></h3>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:title) %>:</strong></dt>
  <dd><%= @project.title %></dd>
  <dt><strong><%= model_class.human_attribute_name(:details) %>:</strong></dt>
  <dd><%= @project.details %></dd>
  <dt><strong><%= model_class.human_attribute_name(:expected_completion_date) %>:</strong></dt>
  <dd><%= @project.expected_completion_date %></dd>
  <dt><strong><%= model_class.human_attribute_name(:tenant_id) %>:</strong></dt>
  <dd><%= @project.tenant_id %></dd>
</dl>

<%= link_to '< Back to Organization Home', root_path, :class => 'btn btn-default' %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
edit_tenant_project_path(project: @project, tenant: @project.tenant_id), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),tenant_project_path(@project, @project.tenant_id),
      :method => 'delete',
      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm",
      :default => 'Are you sure?')) },
      :class => 'btn btn-danger' %>
      <br/>
      <hr>
      <h2>Logs</h2>




     <hr>
     <h2>Pmus</h2>
  <p>
     <%=link_to 'Add a Field',new_tenant_project_pmu_path(@tenant,@project,@pmu)%>
  </p>
  <div>
  </div>


  <hr>
  <h2>globalGAP</h2>
  <hr>

<!-- 

<div class="tabbable"> <!-- Only required for left/right tabs -->
 <!--  <ul class="nav nav-tabs">
    <li class="active"><a href="#tab1" data-toggle="tab">Logs</a></li>
    <li><a href="#tab2" data-toggle="tab">Fields</a></li>
    <li><a href="#tab3" data-toggle="tab">globalGAP</a></li>
  </ul>

 -->
  <!-- <div class="tab-content">
    <div class="tab-pane active" id="tab1">
      <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p> <p>I'm in Logs .</p>
      <div>
        
      </div>
    </div>
    <div class="tab-pane" id="tab2">
      <p>Howdy, I'm in Fields </p>
     <!--  <%=link_to 'Add a Field',new_tenant_project_pmu_path(@tenant,@project,@pmu)%> -->
   <!--  </div>
    <div class="tab-pane" id="tab3">
      <p>Howdy, I'm in globalGAP </p>
    </div>
  </div> --> 
<!-- </div> --> 
