<script src="//maps.google.com/maps/api/js?v=3.23&key=AIzaSyA5Ce2ZGnQx7kxD6z_A8YL_qpYH5yzbqvI"></script>
<script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
<script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script> 


<!-- <div style='width: 100%;'>
  <div id="map" style='width: 100%; height: 400px;'></div>
</div> -->
<div class='row'>
    <div class='col-md-3'>
        <p>
          <strong>Produce:</strong>
          <%= @pmu.produce %>
        </p>
        <p>
          <strong>Village:</strong>
          <%= @pmu.village %>
        </p>

        <p>
          <strong>Location:</strong>
          <%= @pmu.location %>
        </p>

        <p>
          <strong>Size:</strong>
          <%= @pmu.size %>
        </p>

        <p>
          <strong>Plants:</strong>
          <%= @pmu.plants %>
        </p>

        <p>
          <strong>Production:</strong>
          <%= @pmu.production %>
        </p>

        <p>
          <strong>Facilities:</strong>
          <%= @pmu.facilities %>
        </p>

        <p>
          <strong>Certification:</strong>
          <%= @pmu.certification %>
        </p>

        <p>
          <strong>Latitude:</strong>
          <%= @pmu.latitude %>
        </p>

        <p>
          <strong>Longitude:</strong>
          <%= @pmu.longitude %>
        </p>

        <%= link_to 'Edit', edit_tenant_pmu_path(@tenant,@pmu) %> |
        <%= link_to 'Back', tenant_pmus_path %>
                <h5>Nearby Locations</h5>
        <ul>
        <% for pmu in @pmu.nearbys(100) %>
          <li><%= link_to pmu.location, tenant_pmu_path(@tenant,pmu) %> (<%= pmu.distance.round(2) %>km )</li>
        <% end %>
        </ul>

    </div>

      <div class='col-md-9'>
          <div id="map-container">
            <div id="map-canvas"></div>
          </div>
      </div>
    </div>
<!-- <div style='width: 800px;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div> -->


<script>
var mapOptions = {mapTypeId: google.maps.MapTypeId.SATELLITE};
handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id:'map-canvas'}}, function(){
        markers = handler.addMarkers(<%=raw @hash.to_json%>, {draggable: false});
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        handler.getMap().setZoom(18);
});   


</script>
