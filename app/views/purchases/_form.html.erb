<div class='row'>
<%= form_for([@tenant,@purchase]) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

     </div>


 <!-- :selected => params[:pid]  -->
  <div class="form-group">
    <%= f.label :pmu_id,class:'control-label' %><br>
        <%=f.grouped_collection_select(:pmu_id,Grower.where(tenant_id:params[:tenant_id]).order(name: :asc),:pmus,:name,:id,:location,{:prompt=> true,:selected => params[:pid]},{:class=>'form-control'})%>

  </div>
  <div class="form-group">
    <%= f.label :price,class:'control-label' %><br>
    <%= f.text_field :price,class:'form-control' %>
  </div>
   <div class="form-group">
    <%= f.label :analysis,class:'form-label' %><br>
    <%= f.text_field :analysis ,class:'form-control'%>
  </div>
  <div class="form-group">
    <%= f.label :note,class:'form-label' %><br>
    <%= f.text_field :note ,class:'form-control'%>
  </div>
  <div class="form-group">
    <%= f.hidden_field :tenant_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= link_to 'Back', tenant_purchases_path %>
</div>

